/* eslint-disable react/prop-types */
const Balance = ({ value }) => {
  return (
    <div>
      <p className="font-medium text-xl">Your balnce is Rs {value}</p>
    </div>
  );
};

export default Balance;
